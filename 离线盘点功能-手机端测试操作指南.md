# 离线盘点功能 — 手机端测试/操作指南

## 一、前置准备

**环境要求：**
- 后端服务已启动，且部署了 `GET /wms/checkOrder/offline/snapshot/{id}` 接口
- App 已安装到手机/模拟器
- 仓库中已有库存数据（至少 1 个仓库内有器材实例）

---

## 二、完整测试流程

### 阶段 A：Web 端准备盘点单（有网）

```
1. 登录 Web 管理端
2. 进入「盘点管理」→ 新增盘点单
   - 选择仓库（必须）
   - 可选库区/货架缩小范围
   - 指定盘点人
3. 提交盘点单 → 状态变为「待盘点」
4. 点击「开始盘点」按钮（触发 startCheck，生成 SKU 明细快照）
```

> ⚠️ **必须先"开始盘点"**，否则 App 下载快照时会提示"盘点单尚未生成明细"。

---

### 阶段 B：App 端下载快照（有网）

```
1. 打开 WMS App → 登录
2. 首页点击「离线盘点」卡片（或在功能列表中找到）
3. 进入离线盘点页，默认显示「可下载」Tab
4. 列表展示所有状态为「待盘点」的盘点单
5. 找到目标盘点单 → 点击「⬇ 下载数据」
6. 等待下载完成，提示「下载成功」后自动切换到「已下载」Tab
```

**验证点：**
- ✅ 「已下载」Tab 右上角显示任务数量角标
- ✅ 任务卡片显示：盘点单号、盘点范围、下载时间、状态为「待盘点」（绿色标签）
- ✅ 断网后重新打开 App，任务仍在「已下载」列表中（本地持久化验证）

---

### 阶段 C：离线扫码盘点（模拟无网环境）

**模拟断网方式（二选一）：**
- �� 真机：开启飞行模式
- 💻 模拟器/浏览器调试：DevTools → Network → 切换为 Offline

```
1. 在「已下载」Tab 找到任务 → 点击「开始盘点」
2. 顶部状态条显示黄色：「离线模式 — 扫码数据已本地保存」
3. 点击「📷 扫码」或「🔄 连续扫码」
4. 扫描器材二维码（或手动输入 instanceCode 测试）
```

**验证点（每次扫码后检查）：**

| 场景 | 预期反馈 |
|------|----------|
| 扫到账面内的器材 | 绿色卡片 ✓ + 器材名称，进度条 +1 |
| 扫到不在范围内的码 | 黄色卡片 ⚠「不在盘点范围内（盘盈）」 |
| 重复扫同一个码 | 灰色卡片 ↺「该器材已扫描过」，重复计数 +1 |
| 盘亏（账面有但未扫） | 盘亏数 = 账面总数 - 已扫数，实时显示 |

```
5. 扫码中途退出 App（模拟崩溃）
6. 重新进入离线盘点 → 点击「继续盘点」
```

**验证点（崩溃恢复）：**
- ✅ 已扫数量与退出前一致（`saveProgress` 每次扫码后实时落盘）
- ✅ 盘盈列表完整保留

---

### 阶段 D：联网上传结果

```
1. 恢复网络连接（关闭飞行模式）
2. 顶部状态条变为绿色：「已联网，可上传盘点结果」
3. 两种上传方式：
   - 方式A：在扫码页底部点击「⬆ 完成盘点（上传）」
   - 方式B：返回「已下载」列表 → 点击「⬆ 上传结果」
4. 弹出确认框，显示扫码数量 → 点击确认
5. 提示「上传成功」，自动返回列表
```

**验证点：**
- ✅ 任务状态变为「已上传」（灰色标签）
- ✅ Web 端查看该盘点单 → 状态为已盘点，显示盈亏数据
- ✅ Web 端「盘点详情」中：每个 SKU 的账面数量、实盘数量、盘盈/盘亏数据正确

---

## 三、关键边界测试

### 3.1 重复上传防护

```
1. 已成功上传的任务
2. 再次点击「上传结果」
```
**预期：** 上传后状态变为「已上传」，进度数据已清理，再次上传时提示「暂无扫码数据」

### 3.2 删除本地任务

```
1. 「已下载」Tab → 某任务 → 点击「删除」
2. 确认删除
```
**预期：** 任务从列表消失；Web 端盘点单不受影响（仍为待盘点状态）

### 3.3 重新下载快照

```
1. 「可下载」Tab 中找到已下载过的盘点单
2. 按钮显示「重新下载」（而非「下载数据」）
3. 点击重新下载
```
**预期：** 旧快照被覆盖，已扫进度被清空

### 3.4 大数据量性能验证

```
准备：在 Web 端创建一个包含 500+ 实例的盘点单
1. 下载快照，观察下载耗时（预期 < 3秒）
2. 连续扫码 100 次，观察页面响应（预期无卡顿）
3. 上传结果，观察上传耗时（预期 < 5秒）
```

---

## 四、数据一致性验证表

在 Web 端核对以下数据（上传后）：

| 验证项 | Web 端预期值 |
|--------|-------------|
| SKU 明细数量 | = 快照中 skus 数组长度 |
| 某 SKU 账面数量 | = 快照中该 SKU 的 instances 数量 |
| 某 SKU 实盘数量 | = App 扫到的该 SKU 下编码数 |
| 盘盈总数 | = App 中 surplusCodes 数量 |
| 盘亏总数 | = 账面总数 - 已扫有效码数量 |
| 盘点单盈亏总数 | = 盘盈 - 盘亏 |

---

## 五、常见问题排查

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 下载快照提示"盘点单尚未生成明细" | 未执行 startCheck | Web 端点击"开始盘点" |
| 下载失败，网络错误 | 接口未部署或权限不足 | 确认后端部署了 `/offline/snapshot/{id}` 且用户有 `wms:check:all` 权限 |
| 上传后 Web 端盈亏数据为 0 | scannedInstanceCodes 未包含盘盈码 | 确认 App 上传时合并了 surplusCodes |
| 扫码后进度不恢复 | 本地存储被清理 | 不要清除 App 缓存/数据 |
| "完成盘点"按钮置灰 | 无网络或无扫码数据 | 检查网络连接，至少扫一个码 |

---

## 六、调试技巧

> **提示**：测试阶段建议先在浏览器 DevTools 的 Application → Local Storage 中查看以下三个 Key，直观验证本地持久化是否正常：
> - `offline_check_tasks` — 任务元信息列表
> - `offline_check_{id}_snapshot` — 快照原始数据
> - `offline_check_{id}_progress` — 扫码进度（scannedCodes / surplusCodes）

---

## 七、功能架构概览

```
Web端创建盘点单 → 点击"开始盘点"(startCheck) → 状态变为待盘点
    ↓
App离线盘点页 → 选择盘点单 → 下载快照到本地（一次性全量）
    ↓
进入无网仓库 → 扫码/连续扫码 → 本地 Set O(1) 比对 → 实时显示盘盈/盘亏
    ↓
回到有网区域 → 点击"完成盘点" → 上传 scannedInstanceCodes → 后端自动计算差异
```

**与在线盘点的关系：**
- 在线盘点（`pages/check/edit.vue`）：实时调用 `verifyCodes` 验码，需要网络
- 离线盘点（`pages/offline-check/`）：本地 Set 比对，无需网络，结果联网后批量上传
- 两者共享同一批盘点单数据和后端 `check` 接口，流程隔离但数据互通
